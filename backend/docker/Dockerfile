# Используйте официальный образ Redis
FROM redis:latest

# Установите порт, который будет слушать Redis-сервер (по умолчанию 6379)
EXPOSE 6379

# Команда, которая будет выполнена при запуске контейнера
CMD ["redis-server"]
